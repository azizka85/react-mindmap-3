import styled from "@emotion/styled";

export const MindMapTool = styled.svg({
  width: '1.5rem',
  height: '1.5rem',
  margin: '0.25rem 0.125rem',
  cursor: 'pointer',
  '&:hover': {
    fill: 'orange'
  }
});